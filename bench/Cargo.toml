[package]
name = "orthotope-bench"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
jemallocator = "0.5.4"
mimalloc = { version = "0.1.43", default-features = false }
orthotope = { path = ".." }
